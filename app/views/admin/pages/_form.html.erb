<%= simple_form_for [:admin, @page] do |f| %>
  <%= f.input :title %>
  <%= f.input :text, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} } %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>